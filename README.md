# mysh
A lightweight shell.

## Supported commands
```shell
ls, echo
```

## TODO
```shell
cat, mkdir, rm, cd, pwd, wc, help
```

```shell
redirection (>, >>), pipe (|), multiple pipes, background process (&)
```