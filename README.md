# mysh
A lightweight shell.

## Supported commands
```shell
ls, echo, cat, mkdir, rm, cd, pwd, wc, clear, sleep, man
```

## TODO
```shell
redirection (>, >>), pipe (|), multiple pipes, background process (&)
```
