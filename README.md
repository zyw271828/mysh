# mysh
A lightweight shell.

## Supported commands
```shell
ls, echo, cat, mkdir, rm, cd, pwd, wc
```

## TODO
```shell
help
```

```shell
redirection (>, >>), pipe (|), multiple pipes, background process (&)
```
