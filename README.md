# mysh
A lightweight shell.

## Supported commands
```shell
ls, echo, cat, mkdir
```

## TODO
```shell
rm, cd, pwd, wc, help
```

```shell
redirection (>, >>), pipe (|), multiple pipes, background process (&)
```
