# mysh
A lightweight shell.

## Supported commands
```shell
ls, echo, cat, mkdir, rm
```

## TODO
```shell
cd, pwd, wc, help
```

```shell
redirection (>, >>), pipe (|), multiple pipes, background process (&)
```
