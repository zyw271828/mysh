# mysh
A lightweight shell.

## Supported commands
```shell
ls, echo, cat, mkdir, rm, wc
```

## TODO
```shell
cd, pwd, help
```

```shell
redirection (>, >>), pipe (|), multiple pipes, background process (&)
```
